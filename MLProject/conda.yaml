conda_yaml = """
name: telco-churn-env

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - mlflow
"""

with open("/content/Workflow-CI/MLProject/conda.yaml", "w") as f:
    f.write(conda_yaml)

print("conda.yaml berhasil dibuat")